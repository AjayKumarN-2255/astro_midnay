---
import Layout from "../layouts/Layout.astro";

import Banner from "../components/contact/Banner.astro";
import Form from "../components/contact/Form.astro";
import Map from "../components/contact/Map.astro";

import { fetchPageBySlug } from "../lib/getPageData";
import { extractContactSections } from "../lib/extract/extractContact";
import { load } from "cheerio";

const page = await fetchPageBySlug("contact");
const $ = load(page.content);
const { bannerContent, mapContent } = extractContactSections($);
---

<Layout>
    <Banner bannerContent={bannerContent} />
    <!-- overall container -->
    <main class="w-full">
        <div
            class="w-[92%] sm:w-[90%] md:w-[92%] xl:max-w-[1300px] 2xl:max-w-[1600px] mx-auto"
        >
            <Form />
            <!-- <div
                class="max-w-[950px] bg-card w-full flex flex-col
                 mt-div_sm_gap lg:mt-div_lg_gap rounded-[30px]
                 mb-[140px] lg:mb-[220px]
                 mx-auto"
            >
                <div
                    class="mx-auto w-fit flex flex-col justify-center my-4 md:my-8"
                >
                    <p
                        class="text-primary text-xl text-center font-handwriting"
                    >
                        Contact with us
                    </p>
                    <h1
                        class="font-nunito text-xl md:text-4xl lg:text-5xl mt-1 text-heading text-center"
                    >
                        Feel free to write us <br /> anytime
                    </h1>
                </div>
                <div class="w-full py-4 px-6 md:px-8 flex flex-col">
                    <iframe
                        src="http://localhost:10038/contact-form"
                        width="100%"
                        height="700"
                        style="border:none;"></iframe>
                </div>
            </div> -->
        </div>
    </main>
    <Map mapContent={mapContent} />
</Layout>
